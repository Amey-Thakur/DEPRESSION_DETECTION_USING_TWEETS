# ==============================================================================
# PROJECT: DEPRESSION-DETECTION-USING-TWEETS
# DESCRIPTION: Git attributes for language classification and LFS tracking.
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. LINE ENDINGS & TEXT NORMALIZATION
# ------------------------------------------------------------------------------
# Handle line endings automatically and perform LF normalization on checkin
* text=auto eol=lf

# Explicitly mark source files as text
*.py    text
*.ipynb text
*.md    text
*.txt   text
*.html  text
*.css   text
*.js    text
*.json  text
*.xml   text
*.yml   text

# ------------------------------------------------------------------------------
# 2. BINARY FILES & LFS TRACKING (Hugging Face Compatibility)
# ------------------------------------------------------------------------------
# Models and data files
*.pkl   filter=lfs diff=lfs merge=lfs -text
*.h5    filter=lfs diff=lfs merge=lfs -text
*.sav   filter=lfs diff=lfs merge=lfs -text

# Media and Image assets
*.png   filter=lfs diff=lfs merge=lfs -text
*.jpg   filter=lfs diff=lfs merge=lfs -text
*.jpeg  filter=lfs diff=lfs merge=lfs -text
*.gif   filter=lfs diff=lfs merge=lfs -text
*.ico   filter=lfs diff=lfs merge=lfs -text
*.pdf   filter=lfs diff=lfs merge=lfs -text
*.mp3   filter=lfs diff=lfs merge=lfs -text
*.wav   filter=lfs diff=lfs merge=lfs -text

# ------------------------------------------------------------------------------
# 3. LINGUIST (GitHub Language Statistics)
# ------------------------------------------------------------------------------
# Mark specific files as documentation for cleaner project statistics
*.md    linguist-documentation
*.ipynb linguist-documentation
